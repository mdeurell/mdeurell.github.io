# Critical Earth — Python Dependencies
# Install: pip install -r requirements.txt

# Core data
pandas>=2.1
numpy>=1.24
openpyxl>=3.1          # Read USGS .xls/.xlsx files

# Geospatial
geopandas>=0.14
fiona>=1.9             # GDB file reading (GDAL backend)
shapely>=2.0
pyproj>=3.6

# Visualization
plotly>=5.18
folium>=0.15
matplotlib>=3.8
seaborn>=0.13

# Jupyter
jupyter>=1.0
notebook>=7.0
nbformat>=5.9

# Dev server
flask>=3.0             # Local preview server (python main.py)

# Utilities
requests>=2.31         # Download files / Comtrade API
comtradeapicall>=1.2   # UN Comtrade API wrapper (pip install comtradeapicall)
tqdm>=4.66             # Progress bars for data processing

# Site assembly (build_index.py)
jinja2>=3.1            # Templating for index.html
pyyaml>=6.0            # Section content lives in content/sections/*.yaml
mistune>=3.0           # Markdown -> HTML for prose blocks
watchdog>=3.0          # --watch mode for build_index.py
