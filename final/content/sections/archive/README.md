# Archived section YAMLs

These files are **not rendered** by `pipeline/build/build_index.py` — the
build only loads files listed under `sections:` in `content/site.yaml`. They
live here as historical reference / future reuse, not as part of the live
narrative.

| File | Why archived |
|---|---|
| `003_attractive.yaml` | Folded into `03_new_section.yaml` (the upstream / midstream / downstream consolidation). |
| `003_processing.yaml` | Folded into `03_new_section.yaml`. |
| `11_methodology.yaml` | Never wired into `site.yaml`; methodology lives in the explainer notebook instead. |

To revive a file: move it back into `content/sections/` and add it to
`site.yaml`'s `sections:` list at the position you want it rendered.
